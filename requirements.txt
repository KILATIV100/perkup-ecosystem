# Telegram Bot Framework (aiogram 3.x - the modern choice)
aiogram==3.*

# Database (PostgreSQL is standard for serious projects)
# We need async ORM and driver
SQLAlchemy==2.*
asyncpg==0.28.*

# Configuration/Settings management (modern approach)
pydantic==2.*
python-dotenv==1.*

# Utility
loguru==0.7.*
httpx==0.27.*
