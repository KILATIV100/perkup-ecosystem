[project]
name = "perkup-backend"
version = "1.0.0"
description = "PerkUP Backend API"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
